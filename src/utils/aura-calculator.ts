export function calculateAuraPoints(language: string) {
  // Repo count
  // Language basis
  // Following, followers
  // Badges
  // Gists created
  // Forks
  // Stars
  // Coding streak
  // Issues opened / closed
  // PRs opened / closed

  // Formula: / 100

  const formula =
    (repoCount +
      languageBasis +
      followers +
      badges +
      gists +
      forks +
      stars +
      streak +
      issues +
      prs) /
    100;

}

//Basic user info fetch
// Repo info fetch
